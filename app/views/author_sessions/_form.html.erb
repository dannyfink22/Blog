<%= form_for(@author_session) do |f| %>
  <% if @author_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@author_session.errors.count, "error") %> prohibited this author_session from being saved:</h2>

      <ul>
      <% @author_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
